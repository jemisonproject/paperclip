# ---------------------------------------------------------------
# Paperclip + Tailscale environment variables
# ---------------------------------------------------------------
# Copy this file to .env on the NAS (same folder as docker-compose.yml)
# and fill in real values. Never commit .env.
# ---------------------------------------------------------------

# ---- Tailscale ------------------------------------------------
# Generate a reusable auth key at:
#   https://login.tailscale.com/admin/settings/keys
# - Reusable: yes (so re-creating the container doesn't need a new key)
# - Ephemeral: no (we want a stable device)
# - Tags: tag:paperclip (define this tag in your tailnet ACL first)
TS_AUTHKEY=tskey-auth-REPLACE-WITH-YOUR-KEY

# ---- Paperclip ------------------------------------------------
# Public URL Paperclip uses for invite / login links. Set this to
# the Tailscale MagicDNS hostname Paperclip ends up at — you can
# get it after the first `docker compose up` by running:
#   docker exec paperclip-tailscale tailscale status
# It will look like: paperclip.tail-xxxx.ts.net
PAPERCLIP_PUBLIC_URL=https://paperclip.your-tailnet.ts.net

# Random secret used by better-auth (the auth library Paperclip uses)
# to sign sessions, cookies, and tokens. Generate with:
#   openssl rand -hex 32
BETTER_AUTH_SECRET=replace-with-32-byte-random-hex

# ---------------------------------------------------------------
# Notes
# ---------------------------------------------------------------
# - Admin user is created on first boot through the web UI, not via
#   an env var. Open the Tailscale URL in your browser after the
#   stack is up and follow the signup flow.
# - Each teammate sets their own Anthropic / OpenAI key per-agent
#   inside Paperclip's UI, so usage bills against their own plan.
#   We do NOT put a team-wide LLM key in this file.
